import { styled } from '@stitches/react';

export const HeaderWrapper = styled('div', {
  background: '#18AD86',
  lineHeight: '55px',
  width: '100%',
  top: '0px',
  position: 'fixed',
  textAlign: 'center',
});
